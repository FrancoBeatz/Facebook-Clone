# Env Example for SocialSphere Pro full-stack integrations

# Firebase Client Configuration (auto-configured by the environment)
# Firebase Firestore database and auth keys are handled internally.

# Google Gemini API secure key (accessed on server side)
GEMINI_API_KEY=
